-- database: ./a.db

CREATE TABLE users (
    user_id INTEGER,
    email TEXT NOT NULL UNIQUE,
    name TEXT NOT NULL,
    PRIMARY KEY (user_id)
);
CREATE TABLE orders (
    order_id INTEGER,
    user_id INTEGER,
    amount REAL,
    PRIMARY KEY (order_id, user_id)
);