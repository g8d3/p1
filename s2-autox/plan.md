code to automate reading my twitter feed on s2-autox/read.py

I put my user and pass in X_USER, X_PASSWORD

I want to check if an LLM is able to predict which tweets I would like, save predictions in a graph DB.

