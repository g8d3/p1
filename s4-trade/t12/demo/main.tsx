import React from 'react'
import ReactDOM from 'react-dom/client'
import { WalletDemo } from '../src/index'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WalletDemo />
  </React.StrictMode>,
)