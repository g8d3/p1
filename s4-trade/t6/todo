generate many trading ideas and backtest:

get top %change coins, filter or sort by volume
get %change outliers
get vol outliers
get support and resistance
get fib extensions
set DCA trade, backtest parameters range
set multi entry trade, backtest parameters range